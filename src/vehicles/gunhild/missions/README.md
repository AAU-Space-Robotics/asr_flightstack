# Missions — Gunnhild

Gunnhild-specific mission scripts (`*.py`) go here; they are installed to
`lib/gunnhild/missions/`. Missions are intentionally per-vehicle — see the
`thyra` package for examples.
